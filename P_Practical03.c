#include <stdio.h>
    void main()
        {
          	int sum=0;
          	int n;
          	printf("enter the number Till which sum is need to be calculated: %d ",n);
          	
            for(int i=0;i<=n;i++)
                {
          		      sum = sum +i;
          	    }
          	printf("sum of the numbers will be :%d",sum);
        }
